# Clean product URLs — e.g. /product/vogue -> product.html?slug=vogue
# Using /product/:slug prefix avoids conflicts with static pages like /sizeguide, /policies, etc.
# Static pages are served normally by CF Pages' pretty URL feature.
/product/:slug  /product.html?slug=:slug  200
