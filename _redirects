# Clean product URLs — e.g. /vogue → product.html?slug=vogue
# Cloudflare Pages serves static files first, so index.html, admin.html, etc. still work.
# Functions at /api/* are handled by Cloudflare Pages file-based routing (functions/api/).
# This catch-all only fires for paths that don't match a real file or function.
/*  /product.html?slug=:splat  200
